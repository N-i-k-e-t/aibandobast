# Database
DATABASE_URL="file:./dev.db"

# Authentication - CHANGE THESE IN PRODUCTION
ADMIN_USER="cpofficenashik2025"
ADMIN_PASS="CPOFFICE@nashik2025"

# Session secret - generate a random string for production (min 32 chars)
# You can generate one with: openssl rand -base64 32
SESSION_SECRET="change-this-to-a-random-32-character-string"

# Google Maps API Keys
# Get these from Google Cloud Console: https://console.cloud.google.com
# 
# Frontend key - restrict by HTTP referrer in Google Cloud Console
# Enable: Maps JavaScript API
NEXT_PUBLIC_GOOGLE_MAPS_KEY=""

# Backend key - restrict by IP/API in Google Cloud Console
# Enable: Geocoding API, Directions API (optional), Places API (optional)
GOOGLE_MAPS_SERVER_KEY=""

# OpenAI API Key - server-side only, never expose to client
# Get from: https://platform.openai.com/api-keys
OPENAI_API_KEY=""

# App URL (change for production)
NEXT_PUBLIC_APP_URL="http://localhost:3000"
